# Learn more about services, parameters and containers at
# https://symfony.com/doc/current/service_container.html
parameters:
    #parameter_name: value

services:
    # default configuration for services in *this* file
    _defaults:
        # automatically injects dependencies in your services
        autowire: true
        # automatically registers your services as commands, event subscribers, etc.
        autoconfigure: true
        # this means you cannot fetch services directly from the container via $container->get()
        # if you need to do this, you can override this setting on individual services
        public: false

    # makes classes in src/AppBundle available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    Kvik\AdminBundle\:
        resource: '../../src/Kvik/AdminBundle/*'
        # you can exclude directories or files
        # but if a service is unused, it's removed anyway
        exclude: '../../src/Kvik/AdminBundle/{Entity,Repository,Tests}'

    # controllers are imported separately to make sure they're public
    # and have a tag that allows actions to type-hint services
    Kvik\AdminBundle\Controller\:
        resource: '../../src/Kvik/AdminBundle/Controller'
        public: true
        tags: ['controller.service_arguments']

    # add more services, or override services that need manual wiring
    # AppBundle\Service\ExampleService:
    #     arguments:
    #         $someArgument: 'some_value'
    app.form.registration:
        class: Kvik\AdminBundle\Form\UserType
        tags:
            - { name: form.type, alias: app_user_registration }

    kvik.userManager:
        class: Kvik\AdminBundle\Services\UserManager
        public: true

    kvik.sanitize:
        class: Kvik\AdminBundle\Services\Sanitize
        public: true

    kvik.postManager:
        class: Kvik\AdminBundle\Services\PostManager
        public: true

    kvik.userlogged:
        class: Kvik\AdminBundle\EventListener\LoggedUserListener
        tags:
            - { name: kernel.event_listener, event: kernel.request }
    